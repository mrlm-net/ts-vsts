import * as React from "react";
import * as ReactDOM from "react-dom";
import * as SDK from 'azure-devops-extension-sdk';

export async function showRootComponent(
    ContributionEntry: () => React.ReactElement<any>
) {

}